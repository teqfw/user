/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class TeqFw_User_Shared_Defaults {

    NAME = '@teqfw/user'; // plugin's node in 'teqfw.json' & './cfg/local.json'

    constructor() {
        Object.freeze(this);
    }
}
